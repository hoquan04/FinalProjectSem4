@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model AdminWeb.Areas.Admin.Models.Shipping
@{
    ViewData["Title"] = "Xác nhận xóa Shipping";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="row">
    <div class="col-lg-12">
        <div class="ibox">
            <div class="ibox-title">
                <h5>@ViewData["Title"]</h5>
                <div class="ibox-tools">
                    <a asp-action="Index" class="btn btn-sm btn-secondary">
                        <i class="fa fa-arrow-left"></i> Quay lại danh sách
                    </a>
                </div>
            </div>
            <div class="ibox-content">

                <div class="alert alert-danger">
                    <h4><i class="fa fa-exclamation-triangle"></i> Cảnh báo!</h4>
                    <p>Bạn có chắc chắn muốn xóa Shipping này không?</p>
                    <p><strong>Hành động này không thể hoàn tác!</strong></p>
                </div>

                <div class="row">
                    <div class="col-md-8">
                        <dl class="dl-horizontal">
                            <dt><i class="fa fa-id-card"></i> ID:</dt>
                            <dd>
                                <span class="badge badge-primary">@Model.ShippingId</span>
                            </dd>

                            <dt><i class="fa fa-tag"></i> Địa Chỉ:</dt>
                            <dd><strong>@Model.Address</strong></dd>

                            <dt><i class="fa fa-tag"></i> Thành Phố:</dt>
                            <dd><strong>@Model.City</strong></dd>

                            <dt><i class="fa fa-tag"></i> Mã Bưu Chính:</dt>
                            <dd><strong>@Model.PostalCode</strong></dd>

                            <dt><i class="fa fa-tag"></i> Giá Vận Chuyển:</dt>
                            <dd><strong>@Model.ShippingFee</strong></dd>

                            <dt><i class="fa fa-tag"></i> Ngày Dự Kiến:</dt>
                            <dd><strong>@Model.EstimatedDays</strong></dd>
                        </dl>
                    </div>
                </div>

                <div class="hr-line-dashed"></div>

                @* Form để DELETE API *@
                <form asp-action="Delete" method="post">
                    <input asp-for="ShippingId" type="hidden" />
                    @Html.AntiForgeryToken()

                    <div class="form-group">
                        <button type="submit" class="btn btn-danger" id="btnDelete">
                            <i class="fa fa-trash"></i> Xác nhận xóa
                        </button>
                        <a asp-action="Index" class="btn btn-default">
                            <i class="fa fa-times"></i> Hủy
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

</div>

@section Scripts {
    <script>
        console.log('🔧 Delete script loaded');

        // JavaScript để show loading khi submit
        $('#btnDelete').click(function(e) {
            e.preventDefault(); // Ngăn submit mặc định

            console.log('🖱️ Delete button clicked');
            console.log('🎯 CategoryId:', @Model.ShippingId);

            if (!confirm('Bạn có chắc chắn muốn xóa Shipping không?')) {
                console.log('❌ User cancelled delete');
                return false;
            }

            console.log('✅ User confirmed delete');

            var $btn = $(this);
            var $form = $btn.closest('form');

            // Show loading state
            $btn.prop('disabled', true)
                .html('<i class="fa fa-spinner fa-spin"></i> Đang xóa...');

            console.log('🚀 Submitting form to:', $form.attr('action'));
            console.log('📦 Form method:', $form.attr('method'));

            // Submit form manually
            $form.submit();
        });

        // Log form submit event
        $('form').on('submit', function(e) {
            console.log('📤 Form is being submitted');
            console.log('🎯 Form action:', $(this).attr('action'));
            console.log('🎯 Form method:', $(this).attr('method'));

            // Log form data
            var formData = new FormData(this);
            console.log('📋 Form data:');
            for (var pair of formData.entries()) {
                console.log('   ' + pair[0] + ': ' + pair[1]);
            }
        });

        // Thêm timeout để reset button nếu quá lâu
        var deleteTimeout;
        $('form').on('submit', function() {
            deleteTimeout = setTimeout(function() {
                $('#btnDelete').prop('disabled', false)
                              .html('<i class="fa fa-trash"></i> Xác nhận xóa');
                alert('Thao tác mất quá nhiều thời gian. Vui lòng kiểm tra kết nối API và thử lại.');
            }, 30000); // 30 giây timeout
        });

        // Clear timeout khi page unload
        $(window).on('beforeunload', function() {
            if (deleteTimeout) {
                clearTimeout(deleteTimeout);
            }
        });
    </script>
}
