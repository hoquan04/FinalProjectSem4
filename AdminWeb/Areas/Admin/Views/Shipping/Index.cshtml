@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model List<AdminWeb.Areas.Admin.Models.Shipping>
@{
    ViewData["Title"] = "Danh sách shipping";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
    int stt = 1;
}
<div class="col-lg-12">
    <div class="ibox-content">

        @* 🚀 Success/Error Messages *@
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success alert-dismissable">
                <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                <i class="fa fa-check"></i> @TempData["SuccessMessage"]
            </div>
        }
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger alert-dismissable">
                <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                <i class="fa fa-times"></i> @TempData["ErrorMessage"]
            </div>
        }

        <div class="row">
            <form asp-action="Index" method="get" class="form-inline">
                <input type="text" name="recipientName" value="@ViewBag.RecipientName" placeholder="Tên người nhận" class="form-control mr-2" />
                <input type="text" name="phoneNumber" value="@ViewBag.PhoneNumber" placeholder="Số điện thoại" class="form-control mr-2" />
                <input type="text" name="address" value="@ViewBag.Address" placeholder="Địa chỉ" class="form-control mr-2" />
                <input type="text" name="city" value="@ViewBag.City" placeholder="Thành phố" class="form-control mr-2" />
                <input type="text" name="postalCode" value="@ViewBag.PostalCode" placeholder="Mã bưu điện" class="form-control mr-2" />

                <button type="submit" class="btn btn-primary">Tìm kiếm</button>
            </form>

        </div>

        @* Error từ API call *@
        @if (!string.IsNullOrEmpty(ViewBag.Error))
        {
            <div class="alert alert-danger mt-3">
                <strong><i class="fa fa-exclamation-triangle"></i> @ViewBag.Error</strong>
                @if (!string.IsNullOrEmpty(ViewBag.ErrorDetail))
                {
                    <br>
            
                    <small>@ViewBag.ErrorDetail</small>
                }
                <br><small class="text-muted">
                    <strong>Hướng dẫn:</strong> Đảm bảo API đang chạy tại <code>@AdminWeb.Areas.Admin.Data.ApiConstants.ShippingApi</code>
                </small>
            </div>
        }

        <div class="table-responsive mt-3">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>ID</th>
                        <th>Tên người nhận</th>
                        <th>Số điện thoại</th>
                        <th>Email</th>
                        <th>Địa chỉ</th>
                        <th>Thành phố</th>
                        <th>Mã bưu điện</th>
                        <th>Phí vận chuyển</th>
                        <th>Ngày dự kiến</th> 
                        <th>Thao tác</th>

                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@(stt++)</td>
                                <td>
                                    <span class="badge badge-primary">@item.ShippingId</span>
                                </td>
                                <td>@item.RecipientName</td>
                                <td>@item.PhoneNumber</td>
                                <td>@item.Email</td>
                                <td>@item.Address</td>
                                <td>@item.City</td>
                                <td>@item.PostalCode</td>
                                <td>@item.ShippingFee?.ToString("N0") ₫</td>
                                <td>@item.EstimatedDays ngày</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.ShippingId"
                                           class="btn btn-sm btn-warning" title="Chỉnh sửa">
                                            <i class="fa fa-edit"></i> Sửa
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.ShippingId"
                                           class="btn btn-sm btn-danger" title="Xóa">
                                            <i class="fa fa-trash"></i> Xóa
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center py-4">
                                @if (!string.IsNullOrEmpty(ViewBag.SearchString))
                                {
                                    <div class="alert alert-info">
                                        <i class="fa fa-info-circle"></i>
                                        Không tìm thấy shipping nào phù hợp với từ khóa "<strong>@ViewBag.SearchString</strong>"
                                    </div>
                                }
                                else if (!string.IsNullOrEmpty(ViewBag.Error))
                                {
                                    <div class="alert alert-warning">
                                        <i class="fa fa-exclamation-triangle"></i>
                                        Không thể tải danh sách shipping
                                    </div>
                                }
                                else
                                {
                                    <div class="alert alert-info">
                                        <i class="fa fa-info-circle"></i>
                                        Chưa có shipping nào.

                                    </div>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @* Footer info *@
        @if (Model != null && Model.Any())
        {
            <div class="row mt-3">
                <div class="col-sm-6">
                    <small class="text-muted">
                        <i class="fa fa-info-circle"></i>
                        Hiển thị <strong>@Model.Count</strong> shipping
                        @if (!string.IsNullOrEmpty(ViewBag.SearchString))
                        {
                            <span>cho từ khóa "<strong>@ViewBag.SearchString</strong>"</span>
                        }
                    </small>
                </div>
                <div class="col-sm-6 text-right">
                </div>
            </div>
        }
    </div>
</div>
