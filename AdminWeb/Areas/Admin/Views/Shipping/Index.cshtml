@* @using AdminWeb.Areas.Admin.Models
@model PagedResponse<Shipping>

@{
    ViewData["Title"] = "Danh sách shipping";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
    int stt = 1 + (Model.PageNow - 1) * Model.PageSize; // để số thứ tự đúng trang
}

<div class="col-lg-12">
    <div class="ibox-content">
        <div class="ibox-title">
            <h5><i class="fa fa-search"></i> Tìm kiếm thông tin vận chuyển</h5>
        </div>

        <div class="ibox-content">
            <form asp-action="Index" method="get" class="form-inline mb-3">
                <div class="form-group mr-2">
                    <input type="text" name="recipientName" value="@ViewBag.RecipientName" class="form-control" placeholder="Tên người nhận" />
                </div>
                <div class="form-group mr-2">
                    <input type="text" name="phoneNumber" value="@ViewBag.PhoneNumber" class="form-control" placeholder="Số điện thoại" />
                </div>
                <div class="form-group mr-2">
                    <input type="text" name="address" value="@ViewBag.Address" class="form-control" placeholder="Địa chỉ" />
                </div>
                <div class="form-group mr-2">
                    <input type="text" name="city" value="@ViewBag.City" class="form-control" placeholder="Thành phố" />
                </div>
                <div class="form-group mr-2">
                    <input type="text" name="postalCode" value="@ViewBag.PostalCode" class="form-control" placeholder="Mã bưu điện" />
                </div>

                <button type="submit" class="btn btn-primary">
                    <i class="fa fa-search"></i> Tìm kiếm
                </button>
          
            </form>
        </div>

        <div class="table-responsive mt-3">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>ID</th>
                        <th>Tên người nhận</th>
                        <th>Số điện thoại</th>
                        <th>Email</th>
                        <th>Địa chỉ</th>
                        <th>Thành phố</th>
                        <th>Mã bưu điện</th>
                        <th>Phí vận chuyển</th>
                        <th>Ngày dự kiến</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Data != null && Model.Data.Any())
                    {
                        foreach (var item in Model.Data)
                        {
                            <tr>
                                <td>@(stt++)</td>
                                <td><span class="badge badge-primary">@item.ShippingId</span></td>
                                <td>@item.RecipientName</td>
                                <td>@item.PhoneNumber</td>
                                <td>@item.Email</td>
                                <td>@item.Address</td>
                                <td>@item.City</td>
                                <td>@item.PostalCode</td>
                                <td>@item.ShippingFee?.ToString("N0") ₫</td>
                                <td>@item.EstimatedDays ngày</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.ShippingId" class="btn btn-sm btn-warning">
                                            <i class="fa fa-edit"></i> Sửa
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.ShippingId" class="btn btn-sm btn-danger">
                                            <i class="fa fa-trash"></i> Xóa
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="11" class="text-center">Không có shipping nào</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="row mt-3">
            <div class="col-sm-6">
                <nav>
                    <ul class="pagination mb-0">
                        @for (int i = 1; i <= Model.TotalPage; i++)
                        {
                            <li class="page-item @(i == Model.PageNow ? "active" : "")">
                                <a class="page-link" asp-action="Index"
                                   asp-route-pageNow="@i"
                                   asp-route-pageSize="@Model.PageSize">@i</a>
                            </li>
                        }
                    </ul>
                </nav>
            </div>
            <div class="col-sm-6 text-right">
                <small class="text-muted">
                    <i class="fa fa-info-circle"></i>
                    Trang <strong>@Model.PageNow</strong> / <strong>@Model.TotalPage</strong>
                    (Hiển thị <strong>@Model.Data.Count()</strong> / <strong>@Model.TotalCount</strong> shipping)
                </small>
            </div>
        </div>

    </div>
</div> *@
