@model AdminWeb.Areas.Admin.Data.Services.DashboardStatsViewModel
@{
    ViewData["Title"] = "Bảng điều khiển";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">@ViewBag.Error</div>
}

<div class="row">
    <div class="col-lg-3 col-md-6">
        <div class="ibox bg-primary color-white text-center">
            <div class="ibox-title"><h5>Tổng sản phẩm</h5></div>
            <div class="ibox-content">
                <h1 class="no-margins">@Model.TotalProducts</h1>
                <small>Sản phẩm đang kinh doanh</small>
            </div>
        </div>
    </div>

    <div class="col-lg-3 col-md-6">
        <div class="ibox bg-success color-white text-center">
            <div class="ibox-title"><h5>Tổng đơn hàng</h5></div>
            <div class="ibox-content">
                <h1 class="no-margins">@Model.TotalOrders</h1>
                <small>Đơn hàng đã xử lý</small>
            </div>
        </div>
    </div>

    <div class="col-lg-3 col-md-6">
        <div class="ibox bg-warning color-white text-center">
            <div class="ibox-title"><h5>Người dùng</h5></div>
            <div class="ibox-content">
                <h1 class="no-margins">@Model.TotalUsers</h1>
                <small>Khách hàng đăng ký</small>
            </div>
        </div>
    </div>

    <div class="col-lg-3 col-md-6">
        <div class="ibox bg-danger color-white text-center">
            <div class="ibox-title"><h5>Doanh thu</h5></div>
            <div class="ibox-content">
                <h1 class="no-margins">@Model.TotalRevenue.ToString("N0") đ</h1>
                <small>Tổng doanh thu</small>
            </div>
        </div>
    </div>
</div>

<hr />

<div class="row">
    <div class="col-lg-8">
        <div class="ibox">
            <div class="ibox-title"><h5>Doanh thu 12 tháng</h5></div>
            <div class="ibox-content">
                <canvas id="revenueChart" height="160"></canvas>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="ibox">
            <div class="ibox-title"><h5>Tỷ lệ trạng thái đơn hàng</h5></div>
            <div class="ibox-content">
                <canvas id="orderStatusChart" height="160"></canvas>
            </div>
        </div>
    </div>
</div>

<div class="row mt-4">
    <div class="col-lg-12">
        <div class="ibox">
            <div class="ibox-title"><h5>Đơn hàng mới nhất</h5></div>
            <div class="ibox-content">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Mã đơn</th>
                            <th>Khách hàng</th>
                            <th>Ngày đặt</th>
                            <th>Tổng tiền</th>
                            <th>Trạng thái</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.RecentOrders.Any())
                        {
                            foreach (var o in Model.RecentOrders)
                            {
                                <tr>
                                    <td>#@o.OrderId</td>
                                    <td>@(o.Shipping?.RecipientName ?? "N/A")</td>
                                    <td>@o.OrderDate.ToString("dd/MM/yyyy")</td>
                                    <td>@($"{o.TotalAmount:N0} đ")</td>
                                    <td><span class="badge bg-info">@o.Status</span></td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr><td colspan="5" class="text-center text-muted">Không có đơn hàng nào</td></tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@section Styles {
    <style>
        /* 🎨 Ghi đè chữ trắng mặc định trong ibox */
        .ibox.bg-primary,
        .ibox.bg-success,
        .ibox.bg-warning,
        .ibox.bg-danger {
            color: #000 !important; /* chữ đen */
        }

        .ibox-title h5,
        .ibox-content h1,
        .ibox-content small {
            color: #000 !important; /* tiêu đề và nội dung đều đen */
        }

        /* 👇 Nếu bạn muốn giữ nền màu sáng hơn để dễ đọc */
        .ibox.bg-primary {
            background-color: #d9edf7 !important; /* xanh nhạt */
        }

        .ibox.bg-success {
            background-color: #dff0d8 !important; /* xanh lá nhạt */
        }

        .ibox.bg-warning {
            background-color: #fcf8e3 !important; /* vàng nhạt */
        }

        .ibox.bg-danger {
            background-color: #f2dede !important; /* đỏ nhạt */
        }
    </style>
}
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        const orderStatusData = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.OrderStatusStats));
        const monthlyRevenue = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.MonthlyRevenue));

        // 🧠 Map số -> chữ tiếng Việt
        const statusLabels = {
            "0": "Chờ xác nhận",
            "1": "Đã xác nhận",
            "2": "Đang giao",
            "3": "Hoàn tất",
            "4": "Đã hủy"
        };

        // === Biểu đồ trạng thái đơn hàng ===
        new Chart(document.getElementById('orderStatusChart'), {
            type: 'doughnut',
            data: {
                labels: orderStatusData.map(s => statusLabels[s.Status] || "Không xác định"),
                datasets: [{
                    data: orderStatusData.map(s => s.Count),
                    backgroundColor: ['#1ab394', '#f8ac59', '#ed5565', '#1c84c6', '#888']
                }]
            },
            options: {
                plugins: {
                    legend: { position: 'bottom' }
                }
            }
        });

        // === Biểu đồ doanh thu ===
        new Chart(document.getElementById('revenueChart'), {
            type: 'line',
            data: {
                labels: monthlyRevenue.map(m => m.Month),
                datasets: [{
                    label: 'Doanh thu (VNĐ)',
                    data: monthlyRevenue.map(m => m.Revenue),
                    borderColor: '#1ab394',
                    backgroundColor: 'rgba(26,179,148,0.2)',
                    fill: true,
                    tension: 0.3
                }]
            }
        });
    </script>
}
