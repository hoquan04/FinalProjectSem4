@using AdminWeb.Areas.Admin.Models
@model PagedResponse<AdminWeb.Areas.Admin.Models.DTOs.OrderDisplayDto>


@{
    ViewData["Title"] = "Quản lý đơn hàng";
}

<div class="clearfix mb-3" style="display: table; width: 100%;">
    <div style="display: table-cell; vertical-align: middle;">
        <h2 class="m-0">Quản lý đơn hàng</h2>
    </div>
    <div style="display: table-cell; vertical-align: middle; text-align: right;">
        <button type="button" class="btn btn-default btn-sm" title="Làm mới" onclick="location.reload();">
            <i class="fa fa-refresh"></i>
        </button>
    </div>
</div>

<br />

<!-- 🔍 Form tìm kiếm -->
<form asp-action="Search" method="post" class="mb-3">
    <div class="row">
        <div class="col-md-3">
            <input type="text" name="Keyword" class="form-control"
                   placeholder="Tìm kiếm theo mã, tên KH, email..."
                   value="@(ViewBag.SearchModel?.Keyword)" />
        </div>

        <div class="col-md-2">
            <input type="text" name="FromDate" id="FromDate" class="form-control"
                   placeholder="Từ ngày"
                   value="@(ViewBag.SearchModel?.FromDate?.ToString("dd/MM/yyyy"))" />
        </div>

        <div class="col-md-2">
            <input type="text" name="ToDate" id="ToDate" class="form-control"
                   placeholder="Đến ngày"
                   value="@(ViewBag.SearchModel?.ToDate?.ToString("dd/MM/yyyy"))" />
        </div>

        <div class="col-md-2">
            <input type="number" name="MinAmount" class="form-control"
                   placeholder="Từ tổng tiền"
                   value="@(ViewBag.SearchModel?.MinAmount)" />
        </div>

        <div class="col-md-2">
            <input type="number" name="MaxAmount" class="form-control"
                   placeholder="Đến tổng tiền"
                   value="@(ViewBag.SearchModel?.MaxAmount)" />
        </div>

        <div class="col-md-1">
            <button type="submit" class="btn btn-primary btn-block">Tìm</button>
        </div>
    </div>
</form>

<!-- 📋 Bảng danh sách đơn hàng -->
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Mã đơn</th>
            <th>Người nhận</th>
            <th>Email</th>
            <th>Ngày đặt</th>
            <th>Trạng thái</th>
            <th>Tổng tiền</th>
            <th>Địa chỉ</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @if (Model?.Data != null && Model.Data.Any())
        {
            foreach (var order in Model.Data)
            {
                <tr>
                    <td>@order.OrderId</td>
                    <td>@order.RecipientName</td>
                    <td>@order.Email</td>
                    <td>@order.OrderDate.ToString("dd/MM/yyyy")</td>
                    <td>
                        @if (order.Status == "Pending")
                        {
                            <span class="badge bg-warning">Chờ xử lý</span>
                        }
                        else if (order.Status == "Confirmed")
                        {
                            <span class="badge bg-info">Đã xác nhận</span>
                        }
                        else if (order.Status == "Shipping")
                        {
                            <span class="badge bg-primary">Đang giao</span>
                        }
                        else if (order.Status == "Completed")
                        {
                            <span class="badge bg-success">Hoàn tất</span>
                        }
                        else if (order.Status == "Cancelled")
                        {
                            <span class="badge bg-danger">Đã hủy</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">@order.Status</span>
                        }
                    </td>
                    <td>@order.TotalAmount.ToString("N0") đ</td>
                    <td>@order.Address</td>
                    <td>
                        <a asp-area="Admin" asp-controller="Order" asp-action="OrderDetail"
                           asp-route-orderId="@order.OrderId"
                           class="btn btn-sm btn-primary">Chi tiết</a>

                        <a asp-action="Edit" asp-route-id="@order.OrderId"
                           class="btn btn-sm btn-warning">Sửa</a>

                        @* <form asp-action="Delete" asp-route-id="@order.OrderId" method="post" style="display:inline">
                            <button type="submit" class="btn btn-sm btn-danger"
                                    onclick="return confirm('Bạn có chắc chắn muốn xóa đơn này?');">
                                Xóa
                            </button>
                        </form> *@
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="8" class="text-center">Không có đơn hàng nào</td>
            </tr>
        }
    </tbody>
</table>

<!-- 📌 Phân trang -->
@if (Model != null && Model.TotalPage > 1)
{
    <nav>
        <ul class="pagination justify-content-center">
            <li class="page-item @(Model.PageNow == 1 ? "disabled" : "")">
                <a class="page-link" asp-action="Index" asp-route-pageNow="@(Model.PageNow - 1)" asp-route-pageSize="@Model.PageSize">«</a>
            </li>

            @for (int i = 1; i <= Model.TotalPage; i++)
            {
                <li class="page-item @(Model.PageNow == i ? "active" : "")">
                    <a class="page-link" asp-action="Index" asp-route-pageNow="@i" asp-route-pageSize="@Model.PageSize">@i</a>
                </li>
            }

            <li class="page-item @(Model.PageNow == Model.TotalPage ? "disabled" : "")">
                <a class="page-link" asp-action="Index" asp-route-pageNow="@(Model.PageNow + 1)" asp-route-pageSize="@Model.PageSize">»</a>
            </li>
        </ul>
    </nav>
}

@section Scripts {
    <script>
        $(function () {
            $('#FromDate, #ToDate').datepicker({
                format: "dd/mm/yyyy",
                autoclose: true,
                todayHighlight: true,
                language: "vi"
            });
        });
    </script>
}
